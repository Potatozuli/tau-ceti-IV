// Hooks.on("preUpdateToken", (token, updates) => { //Movement de-aiming
//     if((updates.x ?? updates.y) && (token.actor.system.props.Pace !== undefined)){ //If the player has moved

//         speed = Number(token.actor.system.props.Speed);
//         const distMoved = Math.floor(Math.hypot(token.x - updates.x, token.y - updates.y)/canvas.grid.size);
//     }
// });